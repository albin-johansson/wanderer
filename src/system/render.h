#pragma once

#include <renderer.h>

#include "types.h"

namespace wanderer {

void render_player(Registry& registry,
                   centurion::Renderer& renderer,
                   float alpha);

}
