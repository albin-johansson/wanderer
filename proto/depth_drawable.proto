syntax = "proto3";

import "rect.proto";

package wanderer.protobuf;

message depth_drawable {
  optional uint64 texture_index = 1;
  optional irect src = 2;
  optional frect dst = 3;
  optional int32 layer_index = 4;
  optional int32 depth_index = 5;
}