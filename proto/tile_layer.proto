syntax = "proto3";

package wanderer.proto;

message TileRow {
  repeated int32 tiles = 1 [packed = true];
}

message TileMatrix {
  repeated TileRow rows = 2;
}

message TileLayer {
  optional TileMatrix matrix = 1;
  optional int32 z_index = 2;
}